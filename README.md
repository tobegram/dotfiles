The helper shall provide self configured config files and a coupe of shell scripts in order to harmonize shell terminals on difeerent hardware as well as operating systems.

```bash
# use the following command

curl --location --output DotConfigHelper.zip https://github.com/tobegram/DotConfigHelper/archive/refs/heads/main.zip
